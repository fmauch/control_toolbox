name: Coverage Build - Rolling
on:
  workflow_dispatch:
  push:
    branches:
      - ros2-master
  pull_request:
    branches:
      - ros2-master

jobs:
  coverage_rolling:
    name: coverage build - rolling
    uses: ros-controls/ros2_control_ci/.github/workflows/reusable-build-coverage.yml@codecov_v3
    with:
      ros_distro: rolling
